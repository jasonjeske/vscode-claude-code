# Reconcile the invented state workbook

Use only inputs/state-practice.xlsx with existing approved workbook tools. Create a NEW
outputs/state-reconciliation.xlsx; choose a new filename if it exists. Preserve source bytes.

The workbook is wholly invented OH 2026 data, USD. Headers are row 3, detail rows 4-7, row 9 is a
control total. Match exactly on State + Tax year + Property ID. IDs are text; retain leading zeros.
Reject duplicate keys and stop before ambiguous joins. Do not include the control row as detail.
Use Bill minus Book, exact equality for these invented whole-dollar amounts, and full populations.

Include source copies, a matched comparison, unmatched records from each side, and controls.
Distinguish missing matches from numeric zero. Show positive, negative, net, and gross matched
differences. Tie counts and totals to the two original sheets and show the full difference bridge.
Keep causes unknown unless supported. Make formula calculations traceable and use ordinary compatible
Excel features. Report what was recalculated and what must be checked in native Excel.

No macros, refresh, external connections, new dependencies, source edits, postings, payments,
filings, or publishing. Explain one unfamiliar operation briefly and give one independent check.
