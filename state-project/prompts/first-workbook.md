# First workbook task

Use only inputs/state-practice.xlsx. Inspect it read-only with existing approved tools and the
Excel review skill if available. Do not save the workbook, refresh data, or run macros.

Write a NEW outputs/workbook-map.md (use an unused name if it exists). Describe the actual Book
and Bill sheets, header row, detail ranges, key columns, row counts, amounts, and control formulas.
Show one manually checkable source location. Distinguish inspected features from UNVERIFIED ones.
Explain one useful VS Code or Claude operation briefly. If no reader is available, report that
specific blocker without inventing contents or installing software.
